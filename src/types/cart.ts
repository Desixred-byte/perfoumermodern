export type CartItemRow = {
  id: string;
  user_id: string;
  perfume_slug: string;
  size_ml: number;
  quantity: number;
  unit_price: number | string;
  created_at: string;
  updated_at: string;
};
